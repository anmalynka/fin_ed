services:
  # 1. Backend (Using the root main.py proxy)
  - type: web
    name: fin-advisor-backend-v9
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.0"
      - key: PYTHONPATH
        value: .

  # 2. Frontend (Static Site - This WILL ignore main.py)
  - type: static
    name: fin-advisor-frontend-v9
    buildCommand: cd frontend && npm install && npm run build
    publishPath: frontend/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: fin-advisor-backend-v9
          property: host
